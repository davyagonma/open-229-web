# Application web open-229

Application **Next.js** du projet : catalogue, auth Supabase, API `/api/v1`.

Documentation **complète du projet** (vision, stack, env, contribution) : voir le [**README à la racine du dépôt**](../README.md).

### Commandes

```bash
npm install
npm run dev
npm run build
npm run lint
```

Configuration : copier `env.example` vers `.env.local` et suivre le README racine.
